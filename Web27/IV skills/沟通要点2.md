# 要点
```bash
展现自己最好的一部分
尽可能展示自己会的东西
尽可能把话题往自己会的引
做一个没有感情的哄人机
```
---
# 非技术问题
#### 1. 自我介绍
```bash
# boss
你好，我看到贵司这个岗位的条件，很适合我的情况 # 开场
我自学了一年编程 # 只能多不能少。学了多久，怎么学，有没有经验根据情况自己写
我熟练掌握 Java 语言, 以及高级语言特性。 # 会什么技能。语言、框架、数据库是最基础的三块
熟悉 Spring 的框架原理
熟悉 MySQL 数据库的使用
独立实现了 HTTP 服务器和类 MVC 框架 # 项目 1
独立实现了基于 Spring SpringMVC Mybatis 和 MySQL 的论坛项目 # 项目 2
包含用户管理、登陆注册、权限验证、Linux 环境搭建等工作中常见功能 # 功能可以换其他的
能马上上手工作 # 随时待命，能马上制造效益
还有许多其他信息和详细内容，写在了附件简历中 # 结尾
希望可以把简历发给你，进一步了解我的情况

# 电话面试的长一点
去掉 boss 的开头和结尾
根据个人情况，针对 boss 中间部分自行每段多展开一两句
两个项目都要用三句话介绍一下具体有什么功能

# 现场面试最长
去掉 boss 的开头和结尾
根据个人情况，针对 boss 中间部分自行每段多展开几句
然后挑一个你擅长的详细介绍项目，用自己的语言把简历上的项目描述串起来
具体怎么讲项目和演示项目看后面讲故事部分
如果一个说完对方还没打断就说下一个
```
#### 2. 学习方式
```bash
面试官: 你学了多久，有没有报培训班?
我自学了 1 年左右
是在网上看到的书籍推荐 #不要说网课，网课就感觉是培训班。名校公开课可补充说，但要说得出例子
看了a，b，c 书 # 说上课的书名
写了一些项目 # 这是重点，绕回项目介绍
比如 a 项目, b 项目, a 项目是 xxx, b 项目是 yyy # 两句话大致介绍项目

错误例子:
我: 我学了两个月 # 低于1年都是错误的，从第一天听到编程这两个字或者大学学c语言开始到现在都是学习时间。
我: 报了培训班
我：我看了xxx博客 # 太低级
```
#### 3. 薪资
```bash
# 目标薪资 8k-10k
招聘网站上期望薪资填 10k-15k
# 假设一份工作招聘要求 15k-20k
面试前问你，你就说 14k-16k，以最低工资为标准
面试后问，你就说 13k，主动压价，拿到 offer 再说
```
#### 4. 会不会/能不能/行不行
```bash
# 原则是都会都行都能，所有要求全部答应下来，拿到 offer 再说。
# 要表达对这份工作十分满意，对公司十分向往，想立刻加入。
1. 面试官: 能不能加班？对于加班怎么看？

范例：自己刚入行，需要学习的地方还有很多。所以自己会不断学习，而加班对自己来说就是这样的学习机会。
错误例子：不接受加班 # 那就不会有后文了
错误例子：还好 # 还好就是不好

2. 面试官: 能不能下周入职？

范例：可以，我觉得跟面试官交流十分愉快，想立刻加入贵公司。
错误例子：
我：我现在还不能实习 # 先拿到 offer 再说
我：我要考虑一下 # 考虑就是不行

3. 面试官: 我们这里要求有 xx 经验的人，你行吗？

范例：我可以的。我虽然没有 xx 的经验，但我有丰富的 yy 经验，而且实现过 zz，学习能力很强，知识掌握好，基础也不错，我相信我能很快地掌握 xx。
错误例子：
我：可能不行
```
---
# 技术问题
#### 1. 讲故事
```bash
不要按照上课教学顺序说。比如说先实现了一个简单静态页面，再加上了路由分发，再加上了 ORM。直接说这几个模块有什么功能。
不要用代码里的函数名，new，delete等。听不懂。
说得太简略也听不懂。
```
```bash
1. 面试官：介绍一下你的项目。 # 介绍项目这一个问题要更详细描述功能实现，以下是大致思路。
```
```bash
boMVC 项目介绍：
# 事出有因，真实可信吸引人
# 引子
我之前学习了 Spring 框架，能上手实际写功能，但是对背后的原理不是十分清楚。
# 引回项目介绍
最开始是从 HTTP 协议开始，自己实现了一个基于 Socket 的 HTTP 服务器，实现了 HTTP 协议的获取、解析和返回。# 可拓展说 HTTP 协议解析、字节流和编码等
用了一个月，彻底了解了 HTTP 的流程。因为对以前的死知识有了全新的了解，非常兴奋，每天下班后彻夜学习和编程。
初尝甜头后，决定把后续的框架、ORM等统统自己实现一遍。
HTTP 协议中有不同的 path，那么根据不同 path 显示不同的页面就是 web 框架的核心功能。我在 HTTP 服务器之上就自制了一个类 Spring MVC web 框架。
# 按请求顺序和 MVC 三大块详细说
首先控制器层复制路由分发。通过方法引用和 HashMap 把不同的 path 和路由函数注册在字典里，当接收到下层 HTTP 服务器请求之后，抽取出 path 部分，找到对应路由函数并调用。# 可拓展说怎么解析，什么是高阶函数等
模型层用的是基于 MySQL 的自制 ORM，不同参数动态生成不同 SQL 语句，并执行。返回结果后，根调用类实例化对象并放入返回数据。# 可拓展说怎么实现的 sql 语句拼装、对象映射、序列化和反序列化
路由函数调用模型层的 ORM 实现高效数据处理。
视图层用的是 freeMarker。路由函数在处理完数据后，把数据灌入模板，返回最终渲染好的页面。HTTP 服务器最终把渲染好的字符串拼装成一个 HTTP 响应字节流后返回给浏览器。
整个流程就是请求先到 HTTP 服务器，抛给上层框架经过控制器、模型层、视图层的处理，最后回到 HTTP 服务器并返回一个字节流。# 总结流程
这一部分又用了一个多月，但是彻底理清了一个请求发往服务器然后生成动态响应的整个流程，以及 Socket、HTTP、MVC、框架、模板、ORM背后的工作原理。非常开心，终于可以总览全局，并且以后出现任何问题都能迅速的定位到核心问题点。
# 现场面试的话，最后可以演示项目
# 主动拿笔记本给对方现场演示。动图有几个操作就演示几个操作。边演示边讲解。框架项目一定要指 log 给对面看，告诉他这 log 是什么意思。
```
```bash
boCode 项目介绍：
# 印子
为了学习 Web 开发流程和架构，用 Spring MVC, mybatis, Spring Boot 写了论坛项目并根据需求用上Redis。
# 按照简历介绍项目功能
实现了论坛常见功能，
用户模块实现了注册、登陆、权限验证。。。
帖子模块实现了。。。
交互模块上实现了邮件通知。。。。
# 然后介绍亮点功能和技术，越多越好。详细说明是用来干什么的，怎么实现某个功能的
# 可以说的点：
论坛表设计、实现、邮件实现、消息队列用处等。
多线程架构、Redis用处、Tomcat 用处、 Mybatis 的 join 用法, 数据库用处和优化等。
守护进程，日志查看，依赖和配置管理，一键脚本等。
nginx 配置和作用等。
架构，一个请求是怎么被 nginx、tomcat、spring mvc、mysql、redis分别处理的。
服务端Session、加盐、XSRF、XSS等。
为了提高邮件发送速度，项目使用 async 实现邮件发送功能。降低邮件发送路由的响应时间，提升用户访问体验。
# 现场面试的话，最后可以演示项目
# 主动拿笔记本点开部署链接给对方现场演示。动图有几个操作就演示几个操作。边演示边讲解。还可以演示是怎么一键部署的。
```
```bash
2. 面试官：你为何做这个项目？
范例：为了学习原理、流程、架构等等，追本溯源，自己扩写句子。具体参考项目介绍。
错误例子：
我：我就随便做做。
我：我就照着教程做了这个。
```
```bash
3. 面试官：说一下你最骄傲的事情？
面试官：说这个项目的亮点？
范例：终于搞懂了学习原理、流程、架构等等，追本溯源，自己扩写句子。具体参考项目介绍。
错误例子：没什么亮点。# 这过不了 HR 关
```
```bash
4. 面试官：说一下你在项目中遇到的技术难点？
面试官：你开发中遇到的难点？
范例：学习当中没有完整的资料可以参考，网上各说其词。终于通过不断实践、摸索、搜索、查询，再加上废寝忘食般的热情终于完成了项目。自己扩写句子。
范例：看下面不要反问。

错误例子：
我：没有遇到过什么困难。# 这过不了 HR 关
```

#### 2. 问乘法，就从 1+1 讲起。
```bash
这个东西是为了解决什么问题？问题是什么和问题的背景。
具体怎么导致的问题？举例子。
怎么解决的？举例子。
如果想提高怎么做？
```
```bash
面试官：说一说进程和线程的关系。
# 以下大致回答思路，具体长度每个点可以拓展为两倍长度以上）。
# 先总结
进程隔离内存，线程共享内存。调度是以线程为单位。
# 先说进程为了解决什么问题
现代操作系统上能同时执行多个任务。（可以拓展说什么是任务）
为了能方便的管理这些任务，引入了进程的概念。
# 进程怎么解决的
每个进程拥有独立的内存，这样写代码不需要关心内存保护。（可以拓展说虚拟内存）
另一个是进程的运行是被操作系统调度的，写代码的时候不需要担心什么时候让出控制权。这一套机制让代码写起来更方便高效。因为写代码的时候不用关心其他进程是怎么运行的。（可以拓展说繁荣生态和抽象）
# 进程有什么问题
随着cpu技术的发展，出现了多核。那么为了充分利用这些多核心计算资源，就需要用多个进程。（也可以补充说 io 依赖需要多任务）
但是因为进程之间内存是隔离的，那么多进程之间进行数据通信和同步就非常复杂。（可以具体举例怎么复杂）
# 线程怎么解决的
为了能方便的利用多核心 cpu，引入了线程的概念。一个进程可以拥有多个线程，内存的隔离单元还是进程，但是操作系统的调度单元变成了线程。
线程间通信就非常方便，因为共享内存，可以直接调用函数或共享全局变量。（可以拓展说堆和栈）
# 线程有什么问题
但是事物总是有利有弊的，随意共享全局变量会导致数据不一致的问题。（可以拓展说上课存钱取钱的例子）那就需要引入锁来进行数据同步/原子性操作。
# 后续可继续按解决问题这个思路继续往下说线程、线程池、协程、io 多路复用等。
# 这样就可以说得非常非常长，你说得久，面试官就问得少，时间是固定的。
# 并且也显得你非常专业。了解事物背后的原理，描述有条有理承前启后。引人入胜令人信服。

错误例子：进程是最小资源分配单元，线程是最小调度单元。（听起来像背答案）
```
#### 3. 不要说定义，说流程举例子。
```bash
HTTP 协议是无状态的，一个请求发过来不知道是谁发的。
那么为了标识状态，可以加上一些参数，比如在 query 后面加 ?user_id=1。
但是这么加参数很麻烦，因为要自己手动维护，每个 url 都要改。
那么浏览器就提供了一种方便的数据存储发送机制 Cookie。# 可以拓展说 Cookie 是怎么设置和发送的
用了 Cookie 还是有问题，因为可以被伪造。# 可以拓展举例子说如何伪造
那么进引入了 Session，在服务器生成一个随机数，通过 http 响应让浏览器设置一个 cookie session_id，之后浏览器就可以把这个随机数自动发送过来，然后再和服务器存储的随机数和用户id的映射关系找到这个随机数对应的用户，这样就安全地知道这个请求到底是谁发的了。# 可以拓展详细说 Session 的实现和 Session 的不同实现方式

错误例子：
Cookie 是拿来放数据的，Session 是一种安全机制。
```
#### 4. 不要说简单等主观词汇
```bash
面试管：介绍一下你自己做的东西。
范例：参看前面项目描述
错误例子：两个简单的 demo。

上面这样的回答肯定是错误的，任何时候都不要说简单、了解、基本、入门、一些、一点点这样的描述。
说了简单等于告诉对方自己是个菜鸡，这肯定不行的。
简单是一个非常主观的标准，对你简单对其他人未必简单，而且按照我的标准，你的面试官写的东西都很简单，他不也在面你么？
所以只需要描述自己做了什么事情，简单不简单让对方去判断，而不是自己判断。说不定对方还觉得你写的项目很不错呢。
```
#### 5. 碰到会的，尽量展开多说，不要挤牙膏。能给出例子尽可能给出例子。
```bash
面试官: redis 了解吗？
# 范例:
我: 了解 redis。redis 是一个内存数据库，因为数据放内存所以非常快。
主要用它来作为缓存优化热点路由的性能瓶颈。
还可以用它来实现跨进程数据共享，比如实现 CSRF token 和 session。
# 具体可以展开说。
# 比如优化就是怎么 profile，怎么确定性能瓶颈，用了什么工具，怎么跑分来验证猜测正确，如何确定优化方案，怎么实现缓存。
# 比如跨进程就是为什么要跨进程，进程线程区别等等。
# 把自己掌握的东西按照顺序都讲出来，效果会特别好。
错误例子:
我: 了解/不太了解/不怎么了解/了解得不多等等

错误例子:
面试官：了解 redis 吗？
我：了解
面试官：用过 redis 吗？
我：用过
面试官：为什么用 redis？
我：快/别人这么用
```
#### 6. 不要反问，先答了再说。面试官不满意会再问你。
```bash
面试官：你工作当中遇到过什么问题？

范例：
# 只要搭边就可以说，技术问题、协作问题或者规范问题都可以说。
我：之前遇到过一个 xxx 技术问题。
我是经过了 aaa，bbb，ccc 的调试过程定位到了问题，
然后经过 xxx，yyy 的搜索最终在 zzz 库的 issue 里面搜索到了相似问题的官方回答。
这个问题最终发现是 zzz 库的一个 bug，最后通过升级版本解决。
我：# 看前面讲故事例子

错误例子：
我：你指什么问题？
```
#### 7. 玄学问题可以先试探一下。
```bash
面试官：get 和 post 什么区别？

范例：
# 默认对面不懂，讲一个主流答案的梗概。对面没疑问就展开说主流答案，对面有疑问可以说说自己的想法。

我：按照网上的说法，主要有这么几点区别，post 比 get 更安全，get 有长度限制，xxx。# 随便介绍三点，然后停顿一下

如果面试官没有疑问，你就继续补充主流回答
我：安全体现在这几个方面，get 放在 url 里面普通人能看到，get 能被缓存，xxx。yyy 体现在 aaa 方面，bbb。# 自行展开

如果面试官有疑问，这个可能性很小：
面试官：你说一下你自己的理解/这不是我们要的答案/真的吗
我：其实 get post 没什么区别，无非是放数据的地方不一样。post 也不会更安全，直接浏览器抓包就可以看到，安全要靠 https 保证。# 自行展开

错误例子：
我：没有区别。
```

## 不会的问题
#### 1. 不会按照面试官的方式完成， 但是可以通过其他方式完成
```bash
面试官: 写出用于提取一段文本中 url 的正则

范例:
我: 生产级代码我会写一个函数来确保功能的正确性，代码是这么写的。
先干xx，后干yy，最终zz。# 边写边解释

错误例子:
我: 不会 # 没有写出正则代码， 也没有写出函数代码
```
#### 2. 面试官说的东西没有实际上手， 但是知道一点点皮毛
```bash
# 比如问一个工具或者框架
面试官: docker 知道么，会配置么

范例:
# 假设没有用过 docker， 但是听过或者知道这是个什么东西
我:
Docker 是一个虚拟化工具，
# 用过类似的东西一定要说
虚拟化工具的话我用过 VirtualBox。
Docker 并且目前发展很好，生态也非常丰富，
不少团队会使用 docker 进行容器化部署。
没有在工作中使用 docker，但是有看过相关 demo。（如果懂原理的话就补充原理）
如果要使用的话，我会翻阅官方教学资料后直接上手使用。

错误例子:
我: 我没有用过 Docker，不会配置
```

```bash
# 问了一个工具或者某个特定 api
面试官：Java的 ArrayList 排序怎么指定倒序？
范例：
倒序就是排序在判断的时候，把条件反转。
我认为学习核心的在于掌握原理和流程，这种 api 用到的时候查一下字典就好了。

# 问了一个吹上天的概念
面试官：有没有高并发经验？
# 重点在高并发，而不是经验。引回项目。
范例：高并发的核心有两个，一个是能进行扩容和负载均衡。
单机的话，升级硬件后可以根据按需求设置 Tomcat 线程池 的 连接数, 线程数。
多机的话，可以用 nginx 配置多机负载均衡。第二个是要能处理瞬时请求洪峰，
这一个要靠消息队列来进行削峰。缓解压力之后逐步处理。Tomcat、nginx 我的论坛项目都有用上，
后期想要支持高并发的话可以很容易的通过更新几个配置来针对性处理
# 然后说请求来了后，这几个部分在论坛项目是怎么处理请求的

错误例子：
没有。
```

#### 3. 问到的东西真的一点都不会
```bash
# 比如问一个很难的原理和算法
面试官：分布式一致性了解吗？

范例：
# 一点点都不会就说不会好了
这一个我还没有了解到，我记下了，我会回去研究清楚。

错误例子：
被问住了，什么都不说，长时间沉默。
```

## 技术选型
#### 1. 为何不用 x 技术
```bash
面试官：你这个 ORM 项目为何不用反射来实例化对象？
范例：
这个项目是为了学习 ORM 原理，暂时还没有功能因为不用反射从而不能实现。

错误例子：
我：这个反射我不懂。# 没有调研说明做事欠考虑
```

#### 2. 为何用了 x 技术
```bash
面试官：为何用 Mybatis？
范例：
我：因为 Mybatis 可以定制化 SQL, 高级映射, 很方便的把 SQL 查询结果映射成 Java 对象,
免了几乎所有的 JDBC 代码和手动设置参数以及获取结果集。
API 设计得很简洁，文档写得友好，上手速度很快。

错误例子：
我：我看别人也这么用 # 说明你没有思考，人云亦云
```

## 当场做题
包括代码题，设计题等等。

边做题边讲解思路。
先说/写一个能立刻想到的解法，再逐步优化。不要一直沉默。
